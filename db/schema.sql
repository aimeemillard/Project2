-- Drops the todolist if it exists currently --
DROP DATABASE IF EXISTS passport_demo;
-- Creates the "todolist" database --
CREATE DATABASE passport_demo;


-- Drops the todolist if it exists currently --
DROP DATABASE IF EXISTS symptomlist;
-- Creates the "todolist" database --
CREATE DATABASE symptomlist;
